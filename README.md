friendmeat's dotfiles

# Usage

1. Clone repo

```sh
git clone https://github.com/friendmeat/dotfiles.git ~/dotfiles
cd ~/dotfiles
```

2. Link dotfiles
```sh
./link.sh
```

# Development

Add a new file to the `CONFIGS` array in `link.sh` for every dotfile.
